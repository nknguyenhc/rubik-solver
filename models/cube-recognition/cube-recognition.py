def recognise(pil_image):
    """Given a PIL Image, returns the colours of the 9 cells on the face of the Rubik's cube.

    The `pil_image` is an instance of the PIL.Image class, instantiated using the factory method PIL.Image.open.

    The returned value should be a numpy array of 9 x 3,
    where each block of 3 represents the RGB colour of the cell, 
    in each of 3 lines on a Rubik's cube face from left to right.
    """
    pass
